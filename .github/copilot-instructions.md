# Copilot Instructions

## Project Guidelines
- The user's TradeNet Portal project uses SQL Server Express with connection string: Server=.\SQLEXPRESS;Database=TradeNetDb. They want to use SQL Server (not LocalDB) for their database.